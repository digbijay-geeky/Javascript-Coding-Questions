function removeDuplicates(arr){
    const temp=[]
    for(var i=0;i<arr.length;i++){
        if(!temp.includes(arr[i])){
            temp.push(arr[i])
        }
    }
    return temp;
}
const array=[1,2,3,4,4,6,5,4]
console.log(removeDuplicates(array))
